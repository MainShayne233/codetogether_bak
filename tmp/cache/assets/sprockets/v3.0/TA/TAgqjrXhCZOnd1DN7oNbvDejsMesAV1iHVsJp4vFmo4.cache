I"A(function() {
  var ready;

  ready = function() {
    return $('textarea').keyup(function(event) {
      App.coderoom.type($(event.target).attr('id'), $(event.target).val());
      return alert($('input.user').attr('id'));
    });
  };

  $(document).ready(ready);

  $(document).on('page:load', ready);

}).call(this);
:ET