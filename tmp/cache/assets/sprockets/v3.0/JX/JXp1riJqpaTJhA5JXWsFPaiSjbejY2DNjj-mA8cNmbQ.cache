I"(function() {
  var keystrokes, ready, root;

  keystrokes = [];

  ready = function() {
    return $('textarea').keyup(function(event) {
      var worker;
      keystrokes.push(1);
      worker = new Worker("/javascripts/cable_runner.js");
      return worker.postMessage('data');
    });
  };

  $(document).ready(ready);

  $(document).on('page:load', ready);

  root = typeof exports !== "undefined" && exports !== null ? exports : this;

  root.keystrokes = function() {
    return keystrokes;
  };

}).call(this);
:ET