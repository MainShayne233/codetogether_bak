I"h(function() {
  var ready;

  this.keystrokes = [];

  ready = function() {
    return $('textarea').keyup(function(event) {
      var worker;
      keystrokes.push(1);
      worker = new Worker("/javascripts/cable_runner.js");
      return worker.postMessage();
    });
  };

  $(document).ready(ready);

  $(document).on('page:load', ready);

}).call(this);
:ET