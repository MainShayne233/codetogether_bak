I"P(function() {
  App.coderoom = App.cable.subscriptions.create("CoderoomChannel", {
    connected: function() {},
    disconnected: function() {},
    received: function(data) {
      if ($('input.user#' + data.user_id).length === 0) {
        $('textarea#' + data.id).val(data.content);
        return console.log('wrote here');
      } else {
        return consoel.log('didnt write here!');
      }
    },
    type: function(id, content, user_id) {
      return this.perform('type', {
        id: id,
        content: content,
        user_id: user_id
      });
    }
  });

}).call(this);
:ET