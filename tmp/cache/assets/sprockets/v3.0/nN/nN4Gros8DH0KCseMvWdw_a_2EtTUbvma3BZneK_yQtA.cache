I"©(function() {
  var ready;

  ready = function() {
    var codeMirror;
    $('textarea').keyup(function(event) {});
    codeMirror = CodeMirror.fromTextArea($('.code').get(0), {
      lineNumbers: true,
      mode: "text/x-ruby"
    });
    return codeMirror.getTextArea().keyup(function() {
      return console.log('keyup!');
    });
  };

  $(document).ready(ready);

  $(document).on('page:load', ready);

}).call(this);
:ET